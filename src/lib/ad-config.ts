/**
 * Client-safe ad configuration shared between the visual countdown and UI copy.
 * IMPORTANT: these values are cosmetic. The server-side provider
 * (src/lib/ads/demo-provider.ts) is the source of truth for ad duration
 * and completion verification.
 */
export const DEMO_AD_DURATION_SECONDS_CLIENT = 30;
export const ACCESS_HOURS_CLIENT = 24;
